# Configuration file

"minecraft:desert" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=10

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:desert_hills" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=5

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:extreme_hills" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=20

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:forest" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=50

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:forest_hills" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=10

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:ice_flats" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=80

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:ice_mountains" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=10

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:jungle" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=50

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:jungle_hills" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=10

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:mesa" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=30

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:red_sandstone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=red_sandstone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:red_sandstone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=red_sandstone
}


"minecraft:ocean" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=100

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:plains" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=50

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:savanna" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=50

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:swampland" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=30

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:taiga" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=70

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


"minecraft:taiga_hills" {
    # バイオームの生成量を指定します。 [range: 0 ~ 100]
    I:weight=10

    # バイオームの地形を主に構成するブロックのブロック名を指定します。
    S:terrainBlock=minecraft:stone

    # バイオームの地形を主に構成するブロックのメタデータを指定します。
    S:terrainBlockMeta=stone

    # バイオームの地表を構成するブロックのブロック名を指定します。
    S:topBlock=minecraft:stone

    # バイオームの地表を構成するブロックのメタデータを指定します。
    S:topBlockMeta=stone
}


